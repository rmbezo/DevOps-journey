## Types, overflow, underflow, long long, double, vector

int - 32 bite
long long - 64
double - 64, but not 100% accuracy

vector<int> - vector of integers / works smthng like slice
vector<int>* - pointer
vector<int>** - pointer on pointer
... etc


vector<int>& x - link for functions

----
c++      ||         go 
-----
a.size() ||   len(a)
-----
a[i]     ||   a[i]
-----
a[i] = x ||   a[i] = x
-----
