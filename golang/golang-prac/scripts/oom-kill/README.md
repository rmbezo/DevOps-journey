# OOM killer test with docker and docker compose


to start script run :

```
docker compose up -d && docker compose logs -f
```

then you can see how the script works + the error 137 (the OOM killer comes)