deploymentMode: SingleBinary

singleBinary:
  replicas: 1


backend:
  replicas: 0
read:
  replicas: 0
write:
  replicas: 0
simpleScalable:
  enabled: false

loki:
  auth_enabled: false
  storage:
    type: filesystem
    bucketNames:
      chunks: "chunks"

persistence:
  enabled: false