loki:
  auth_enabled: false
  storage:
    bucketNames:
      chunks: "chunks"     
      ruler: "ruler"       
      admin: "admin"       
singleBinary:
  replicas: 1

# без персистентности на старте
persistence:
  enabled: false