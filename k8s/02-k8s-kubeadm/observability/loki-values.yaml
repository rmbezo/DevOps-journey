
simpleScalable:
  enabled: true
  write:
    replicas: 1
  read:
    replicas: 1
  backend:
    enabled: false

loki:
  auth_enabled: false
  useTestSchema: true
  storage:
    bucketNames:
      chunks: "chunks"
      ruler: "ruler"
      admin: "admin"

persistence:
  enabled: false
